#!/bin/bash

CLIENT_IP=$1

echo "POP3(ACCEPT)    locl:${CLIENT_IP}    inet"
echo "POP3S(ACCEPT)   locl:${CLIENT_IP}    inet"
echo "SMTP(ACCEPT)    locl:${CLIENT_IP}    inet"
echo "SMTPS(ACCEPT)   locl:${CLIENT_IP}    inet"
echo "IMAP(ACCEPT)    locl:${CLIENT_IP}    inet"
echo "IMAPS(ACCEPT)   locl:${CLIENT_IP}    inet"
